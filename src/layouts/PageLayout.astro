---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  canonical?: string;
  noindex?: boolean;
  schemas?: object[];
}

const { title, description, ogImage, canonical, noindex, schemas } = Astro.props;
---

<BaseLayout
  title={title}
  description={description}
  ogImage={ogImage}
  canonical={canonical}
  noindex={noindex}
  schemas={schemas}
>
  <main>
    <slot />
  </main>
</BaseLayout>
